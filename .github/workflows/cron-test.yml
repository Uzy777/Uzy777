name: Cron Test

on:
  schedule:
    - cron: "7,17,27,37,47,57 * * * *"

  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Confirm scheduled run
        run: |
          echo "Cron fired successfully"
          echo "Event: ${{ github.event_name }}"
          date -u
